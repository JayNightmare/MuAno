# Changelog

## [Unreleased]

- Fixed note audio playback scheduling so that playback strictly aligns with note start and duration bounds instead of relying on early event-queuing.
- Added timeline auto-scrolling to keep the playhead visible during playback.
- Configured SEO metadata, Open Graph attributes, and a GitHub Actions pipeline to deploy to GitHub Pages (`/MuAno/` base path).
